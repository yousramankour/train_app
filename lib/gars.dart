import 'package:flutter/material.dart';

class GarsPage extends StatelessWidget {
  const GarsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Page Gars")),
      body: Center(child: Text("Bienvenue dans la page Gars")),
    );
  }
}
