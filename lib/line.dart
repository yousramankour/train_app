import 'package:flutter/material.dart';

class LignePage extends StatelessWidget {
  const LignePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Page Ligne")),
      body: Center(child: Text("Bienvenue dans la page Ligne")),
    );
  }
}
